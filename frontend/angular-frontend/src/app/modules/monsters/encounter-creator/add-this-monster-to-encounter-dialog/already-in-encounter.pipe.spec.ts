import { AlreadyInEncounterPipe } from './already-in-encounter.pipe';

describe('AlreadyInEncounterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlreadyInEncounterPipe();
    expect(pipe).toBeTruthy();
  });
});
